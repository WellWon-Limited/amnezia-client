#pragma once

// The product composition only stores a SecureQSettings pointer through the repository header.
// Host syntax checks deliberately do not link the application keychain implementation.
#define SECUREQSETTINGS_H
class SecureQSettings;

