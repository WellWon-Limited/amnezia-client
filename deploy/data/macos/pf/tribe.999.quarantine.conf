# Terminal emergency quarantine.  This anchor is last inside Tribe's root
# anchor and its quick rule cannot be overridden by any normal Tribe pass rule.
# Loopback remains available because 000.allowLoopback sets `skip on lo0`.
block drop quick all flags any no state
